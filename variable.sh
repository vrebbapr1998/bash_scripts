#!/usr/bin/bash
#The above line tells about path of intrepeter.
# declaring variables one, two and three
one=Hello
two="_World."
three=sh
#If the variable contains only string without any special char no double quote as the varable one
#We use $ to avoid errors while executing.
echo "$one$two$three"
